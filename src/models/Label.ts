export class Label {

  public labelId: number

  public labelContent: string

  constructor(
    labelId: number,
    labelContent: string
  ) {
    this.labelId = labelId
    this.labelContent = labelContent
  }
}
